<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="at.ac.tuwien.caa.cvl.imagine"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="19" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!-- Tell the system this app requires OpenGL ES 2.0. -->
    <!-- <uses-feature
        android:glEsVersion="0x00020000"
        android:required="true" /> -->

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher_imagine"
        android:label="@string/app_name"
        android:theme="@style/ImagineActionBarTheme" >
        <activity
            android:name="at.ac.tuwien.caa.cvl.imagine.ui.activity.MainActivity"
            android:label="@string/app_name" >
            <meta-data
                android:name="android.support.UI_OPTIONS"
                android:value="splitActionBarWhenNarrow" />

            <intent-filter>
                <!-- <action android:name="android.intent.action.MAIN" /> -->

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
                
        <activity
            android:name="at.ac.tuwien.caa.cvl.imagine.ui.activity.EditImageActivity"
            android:label="@string/title_activity_edit_image" >
            
             <meta-data
                android:name="android.support.UI_OPTIONS"
                android:value="splitActionBarWhenNarrow" />
            
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
